# Copy to .env and adjust. Local Docker Postgres defaults shown below.
# The whole app reads a single DATABASE_URL.
DATABASE_URL=postgresql://navlens:navlens@localhost:5433/navlens

# FastAPI reads this to know where the DB is (same value as above in prod).
# In production (Neon/Supabase) this becomes the managed Postgres connection string.
